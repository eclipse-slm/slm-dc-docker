---
dependency:
  name: galaxy
driver:
  name: default
platforms:
  - name: centos7
    image_name: CentOS 7 64-bit (Ansible)
    groups: ['ssh']

  - name: centos8
    image_name: CentOS 8 64-bit (Ansible)
    groups: ['ssh']

  - name: centos9
    image_name: CentOS 9 Stream 64-bit
    groups: ['ssh']

  - name: centos10
    image_name: CentOS 10 Stream 64-bit
    groups: ['ssh']
provisioner:
  name: ansible
  playbooks:
    create: ../default/create-os.yml
    destroy: ../default/destroy-os.yml
    prepare: ../default-deploy/prepare.yml
    converge: ../default-deploy/converge.yml
    verify: ../default-deploy/verify.yml
verifier:
  name: ansible
